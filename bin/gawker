#!/usr/bin/env ronin-exec

require 'gawker'

unless ARGV.length == 1
  STDERR.puts "usage: gawker FILE"
  exit -1
end

Gawker.import(ARGV[0]) do |cred|
  if cred.new?
    print_info "Imported #{cred}"
  else
    print_info "Already imported #{cred}"
  end
end
